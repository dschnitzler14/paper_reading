practice_discussion_claims_spec_module_ui <- function(id) {
  ns <- NS(id)
  tagList(
    p("What is a claim and what is a speculation?"),
    
  )
}

practice_discussion_claims_spec_module_server <- function(id) {
  moduleServer(id, function(input, output, session) {
    
  })
}